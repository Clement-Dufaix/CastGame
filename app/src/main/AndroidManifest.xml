<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="pts3.castgame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_cast"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".activities.ChoixTemplateActivity" />

        <activity android:name=".activities.ChoixPersonnageActivity" />

        <activity android:name=".activities.ChoixMethodeActivity" />

        <activity android:name=".activities.ChoixJeuActivity" />

        <activity android:name=".activities.ReponseActivity" />

        <activity android:name=".activities.MapActivity" />

    </application>

</manifest>
